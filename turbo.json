{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "build/**"]
    },
    "bundle": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    }
  }
}
